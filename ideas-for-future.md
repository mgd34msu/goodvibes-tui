# Ideas for Future

## E5: Diff Syntax in Assistant Prose
When the LLM says "change X to Y" in prose text, detect and render as an inline diff block. May be too aggressive with false positives.

## F4: Git-Aware Context
Auto-detect repo, show branch in header, `/diff` shows uncommitted changes. Part of a larger git module planned for later.

## F5: /commit Command
Generate commit message from conversation context, stage + commit. Part of the git module.
